    void getrmdisk(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getfdisk(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getmount(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getunmount(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getdf(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getUsed(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getmkfs(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getlogin(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getlogout(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getmkgrp(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getrmgrp(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getmkusr(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getrmusr(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getchmod(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getcat(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getedit(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getren(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getmkdir(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getcp(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getmv(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getfind(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getchown(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getchgrp(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getunlink(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }
    void getrep(){
        strcpy(unit, "k");
    getAtributos();
    mkdir(size,unit, path, name);
    }